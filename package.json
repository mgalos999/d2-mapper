{
  "name": "@mgalos999/d2-mapper",
  "version": "0.7.0",
  "description": "Generates images of Diablo 2 map data",
  "repository": "https://github.com/mgalos999/d2-mapper",
  "main": "./build/api/server.js",
  "types": "./build/index.d.js",
  "module": "./build/index.js",
  "files": [
    "build",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && node build/api/server.js",
    "generate": "npm run build && node build/cli.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mgalos999",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.10.3",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "canvas": "^2.8.0",
    "cors": "^2.8.5",
    "cross-fetch": "^3.1.4",
    "express": "^4.17.1"
  }
}
